import random
computer = random.randint(1,100)
user = int(input("write any number between 1,100"))
if user == computer:
           print("correct",computer)
if user < 1:
    print("follow the insturction")
if user >100:
    print("follow the instruction")
else:
    print("wrong the number was",computer)

